# 複製呢個檔案做 .env（本機開發用）。千祈唔好將真 key commit 上 git。
# 部署去 Vercel 時，去 Vercel dashboard → Settings → Environment Variables 設定同名變數。

# ---- OpenRouter（攞 Claude） ----
OPENROUTER_API_KEY=
# 揀邊個 model（去 https://openrouter.ai/models 睇 anthropic/ 開頭嘅 slug）
OPENROUTER_MODEL=anthropic/claude-sonnet-4.5

# ---- Supabase（存 task） ----
# 呢兩個係本專案嘅值（publishable key 係 public-safe）
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=sb_publishable_xxx

# ---- 登入保護 ----
# 全公司共用嘅密碼。留空 = 唔擋（方便首次測試）；填咗 = 開 basic auth。
APP_PASSWORD=
